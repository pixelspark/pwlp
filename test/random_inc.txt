for(n=get_length) {
	set_pixel(n-1, 0, 0, 0);
};
loop {
	lucky = random(get_length);
	for(n=get_length) {
		if(n == lucky) {
			p = get_pixel(n);
			r = (p & 0xFF00) >> 8;
			set_pixel(n, r+1, 0, 0);
		}
	};
	blit;
}